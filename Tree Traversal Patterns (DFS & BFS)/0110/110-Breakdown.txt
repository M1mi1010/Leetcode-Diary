Final attempt:
- In the main function call the helper, and check if -1 is returned 
- In the helper: 
  - Check if root is null, return 0
  - Call helper on the left tree and check if left height is -1 (there's an imbalance in the tree)
  - Do the same on the right subtree
  - If the difference between the two tree heights on the node in LRN is > 1 then return -1
  - Else return the actual height of the tree so far plus the current node:
    - Why: 
      The height of the current node's subtree is: take the taller of its two children, and add 1 (for the current node itself). That's just the standard definition of tree height.
      Example: if root.left's subtree has height 2 and root.right's subtree has height 1, then root's own subtree has height max(2,1) + 1 = 3 — makes sense, since you have to climb up one more level to include root itself.

Stats (at time of submission):
- Runtime: 0 ms (Beats 100.00%)
- Memory: 45.44 MB (Beats 77.59%)
- Time taken: 22m 22s
